# Core LangChain & Embeddings
langchain==0.3.27
langchain-community==0.3.30
langchain-core==0.3.76
langchain-huggingface==0.3.1
langchain-text-splitters==0.3.11

# Vector Store — FAISS (kept for backward compat) + Qdrant (new primary)
faiss-cpu>=1.8.0
qdrant-client>=1.9.0

# Transformers / Embeddings / Reranker
transformers>=4.46.2
sentence-transformers>=3.0.1

# Async HTTP (replaces requests for LLM calls)
httpx>=0.27.0
requests>=2.31.0        # still used for Ollama fallback

# FastAPI & Server
fastapi>=0.115.2
uvicorn[standard]>=0.30.0

# Rate Limiting
slowapi>=0.1.9

# Auth (JWT)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Session & Semantic Cache
redis>=5.0.0

# Data & Utilities
numpy>=1.26.4
pandas>=2.2.3
pydantic>=2.9.2
tqdm>=4.66.5

# Environment & Config
python-dotenv>=1.0.1

# Logging
python-json-logger>=2.0.7

# Document Loaders (extended)
unstructured>=0.12.0
docx2txt>=0.8
beautifulsoup4>=4.12.0

# Dev & Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # also used as async test client
ruff>=0.4.0             # linter

# Evaluation (optional — comment out if not running evals)
# ragas>=0.1.0
